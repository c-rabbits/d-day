# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
# Server-only: used for soft-delete (bypasses RLS). Never expose to client.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OAuth (카카오/구글): Supabase Dashboard > Authentication > URL Configuration
# - Redirect URLs에 https://d-day-one.vercel.app/auth/callback (프로덕션), http://localhost:3000/auth/callback (로컬) 추가
# - Authentication > Providers에서 Kakao(구글) 활성화 후 카카오 개발자 콘솔 REST API 키/Client Secret 입력
